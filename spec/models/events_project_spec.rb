require 'rails_helper'

RSpec.describe EventsProject, type: :model do
  describe '#assocations' do
    # it { is_expected.to belong_to :event }
    # it { is_expected.to belong_to :project }
  end
end
